plugins {
    id "java"
    id "java-library"
    id "io.freefair.lombok" version "5.1.1"
    id "maven-publish"
}

group 'com.cadiducho'
version '1.4.1'

publishing {
    publications {
        maven(MavenPublication) {
            groupId = project.group
            artifactId = rootProject.name
            version = project.version

            from components.java
        }
    }
}

sourceCompatibility = JavaVersion.VERSION_1_8

repositories {
    mavenCentral()
    mavenLocal()
    maven {
        url "https://cadiducho.com/repo/"
    }
}

dependencies {
    api group: 'com.cadiducho', name: 'TelegramBotAPI', version: '4.9'

    //Tests
    testImplementation group: 'org.junit.jupiter', name: 'junit-jupiter-api', version: '5.6.2'
    testImplementation group: 'org.junit.jupiter', name: 'junit-jupiter-params', version: '5.6.2'
    testImplementation group: 'org.junit.jupiter', name: 'junit-jupiter-engine', version: '5.6.2'
    testImplementation group: 'org.mockito', name: 'mockito-core', version: '3.5.7'
    testImplementation group: 'org.mockito', name: 'mockito-junit-jupiter', version: '3.5.7'
}

test {
    useJUnitPlatform()
}

tasks.withType(JavaCompile) {
    options.encoding = 'UTF-8'
}